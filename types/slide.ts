export type Data = {
    id: string
    image: string
    thumbnail: string 
    title: string
    category: string
    description: string
}

export type CurrentSlideData = {
    data: Data
    index: number
}